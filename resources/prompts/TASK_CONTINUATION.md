You are continuing a task that is still in progress. Review your prior messages and progress.

You **must** call one of these tools now:
- `complete_task` — if the work is done
- `defer_task` — if you need to wait and retry later
- `fail_task` — if the task cannot be completed
